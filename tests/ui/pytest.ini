[pytest]
markers =
    ui: UI tests using Nova Act (require NOVA_ACT_API_KEY and running Streamlit app)
    integration: tests requiring live AWS credentials
    system: end-to-end system tests against deployed agents
