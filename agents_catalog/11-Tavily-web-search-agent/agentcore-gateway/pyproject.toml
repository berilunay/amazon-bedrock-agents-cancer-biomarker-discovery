[project]
name = "tavily-gateway-example"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "bedrock-agentcore >= 1.0.3",
    "boto3 >= 1.38.0",
    "strands-agents >= 1.18.0",
    "mcp >= 1.0.0",
]
